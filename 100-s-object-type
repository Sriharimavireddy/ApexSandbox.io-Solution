public Boolean isTypeAccount(sObject record)
{
    // code here

    Schema.getSObjectType objName = record.getSObjectType();
    if(objName == Account){
        return true;

    }else{
        return false;
    }
}